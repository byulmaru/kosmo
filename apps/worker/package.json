{
  "name": "@kosmo/worker",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "start": "node --import tsx src/index.ts",
    "test": "pnpm build && node --experimental-test-module-mocks --import tsx --test 'src/**/*.test.ts'"
  },
  "dependencies": {
    "@kosmo/core": "workspace:*",
    "@kosmo/fedify": "workspace:*",
    "@temporalio/worker": "^1.22.0",
    "@temporalio/workflow": "^1.22.0",
    "tsx": "^4.22.4"
  }
}
