type Account implements Node {
  id: ID!
  name: String
}

enum AccountProfileRole {
  ADMIN
  MEMBER
  OWNER
}

enum AccountState {
  ACTIVE
  DISABLED
  SUSPENDED
}

"""RFC9557 formatted string"""
scalar DateTime

enum FollowPolicy {
  APPROVAL_REQUIRED
  OPEN
}

enum FollowState {
  ACCEPTED
  PENDING
  REJECTED
}

interface Node {
  id: ID!
}

enum ProfileState {
  ACTIVE
  DISABLED
  SUSPENDED
}

type Query {
  me: Account
  node(id: ID!): Node
  nodes(ids: [ID!]!): [Node]!
}
