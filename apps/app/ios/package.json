{
  "scripts": {
    "build": "infisical run -- xcodebuild -project Kosmo.xcodeproj -scheme Kosmo -configuration Debug -sdk iphoneos -destination 'generic/platform=iOS' -derivedDataPath build/DerivedData -allowProvisioningUpdates build",
    "run": "node scripts/run.mjs"
  }
}
