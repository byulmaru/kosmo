diff --git a/dist/mod.cjs b/dist/mod.cjs
index e481467..ed2176c 100644
--- a/dist/mod.cjs
+++ b/dist/mod.cjs
@@ -4779,6 +4779,56 @@ const logger = (0, _logtape_logtape.getLogger)([
 	"docloader"
 ]);
 const DEFAULT_MAX_REDIRECTION = 20;
+const MAX_HTML_SIZE = 1024 * 1024;
+class ResponseBodyTooLargeError extends Error {
+	constructor(maxResponseSize) {
+		super(`Response body exceeds the configured limit of ${maxResponseSize} bytes.`);
+		this.name = "ResponseBodyTooLargeError";
+	}
+}
+async function readResponseText(response, maxResponseSize) {
+	if (maxResponseSize == null) return await response.text();
+	const contentLength = response.headers.get("Content-Length");
+	const declaredSize = contentLength == null ? null : Number(contentLength);
+	if (declaredSize != null && Number.isSafeInteger(declaredSize) && declaredSize >= 0 && declaredSize > maxResponseSize) {
+		try {
+			await response.body?.cancel();
+		} catch {
+		}
+		throw new ResponseBodyTooLargeError(maxResponseSize);
+	}
+	const reader = response.body?.getReader();
+	if (reader == null) return await response.text();
+	const chunks = [];
+	let size = 0;
+	try {
+		while (true) {
+			const { done, value } = await reader.read();
+			if (done) break;
+			size += value.byteLength;
+			if (size > maxResponseSize) {
+				try {
+					await reader.cancel();
+				} catch {
+				}
+				throw new ResponseBodyTooLargeError(maxResponseSize);
+			}
+			chunks.push(value);
+		}
+	} finally {
+		reader.releaseLock();
+	}
+	const bytes = new Uint8Array(size);
+	let offset = 0;
+	for (const chunk of chunks) {
+		bytes.set(chunk, offset);
+		offset += chunk.byteLength;
+	}
+	return new TextDecoder().decode(bytes);
+}
+async function readResponseJson(response, maxResponseSize) {
+	return JSON.parse(await readResponseText(response, maxResponseSize));
+}
 /**
 * Gets a {@link RemoteDocument} from the given response.
 * @param url The URL of the document to load.
@@ -4788,7 +4838,7 @@ const DEFAULT_MAX_REDIRECTION = 20;
 * @throws {FetchError} If the response is not OK.
 * @internal
 */
-async function getRemoteDocument(url, response, fetch) {
+async function getRemoteDocument(url, response, fetch, options, maxResponseSize) {
 	const documentUrl = response.url === "" ? url : response.url;
 	const docUrl = new URL(documentUrl);
 	if (!response.ok) {
@@ -4826,15 +4876,14 @@ async function getRemoteDocument(url, response, fetch) {
 						alternateUrl: altUri.href,
 						url: documentUrl
 					});
-					return await fetch(altUri.href);
+					return await fetch(altUri.href, options);
 				}
 			}
 		}
 	}
 	let document;
 	if (!jsonLd && (contentType === "text/html" || contentType?.startsWith("text/html;") || contentType === "application/xhtml+xml" || contentType?.startsWith("application/xhtml+xml;"))) {
-		const MAX_HTML_SIZE = 1024 * 1024;
-		const html = await response.text();
+		const html = await readResponseText(response, Math.min(maxResponseSize ?? MAX_HTML_SIZE, MAX_HTML_SIZE));
 		if (html.length > MAX_HTML_SIZE) {
 			logger.warn("HTML response too large, skipping alternate link discovery: {url}", {
 				url: documentUrl,
@@ -4859,12 +4908,12 @@ async function getRemoteDocument(url, response, fetch) {
 						alternateUrl: attribs.href,
 						url: documentUrl
 					});
-					return await fetch(new URL(attribs.href, docUrl).href);
+					return await fetch(new URL(attribs.href, docUrl).href, options);
 				}
 			}
 			document = JSON.parse(html);
 		}
-	} else document = await response.json();
+	} else document = await readResponseJson(response, maxResponseSize);
 	logger.debug("Fetched document: {status} {url} {headers}", {
 		status: response.status,
 		url: documentUrl,
@@ -4893,7 +4942,7 @@ async function getRemoteDocument(url, response, fetch) {
 * @returns The document loader.
 * @since 1.3.0
 */
-function getDocumentLoader({ allowPrivateAddress, maxRedirection, skipPreloadedContexts, userAgent } = {}) {
+function getDocumentLoader({ allowPrivateAddress, maxRedirection, maxResponseSize, skipPreloadedContexts, userAgent } = {}) {
 	const tracer = _opentelemetry_api.trace.getTracerProvider().getTracer(name, version);
 	const maximumRedirection = maxRedirection ?? DEFAULT_MAX_REDIRECTION;
 	async function load(url, options, redirected = 0, visited = /* @__PURE__ */ new Set()) {
@@ -4948,7 +4997,7 @@ function getDocumentLoader({ allowPrivateAddress, maxRedirection, skipPreloadedC
 					}
 					return await load(redirectUrl, options, redirected + 1, visited);
 				}
-				const result = await getRemoteDocument(currentUrl, response, load);
+				const result = await getRemoteDocument(currentUrl, response, load, options, maxResponseSize);
 				span.setAttribute("docloader.document_url", result.documentUrl);
 				if (result.contextUrl != null) span.setAttribute("docloader.context_url", result.contextUrl);
 				return result;
diff --git a/dist/mod.d.cts b/dist/mod.d.cts
index 4212039..de9b48f 100644
--- a/dist/mod.d.cts
+++ b/dist/mod.d.cts
@@ -144,6 +144,11 @@ interface DocumentLoaderFactoryOptions {
   * @since 2.2.0
   */
   maxRedirection?: number;
+  /**
+  * The maximum response body size in bytes.  If omitted, the response body is
+  * unbounded.
+  */
+  maxResponseSize?: number;
 }
 /**
 * A factory function that creates an authenticated {@link DocumentLoader} for
@@ -168,7 +173,7 @@ type AuthenticatedDocumentLoaderFactory = (identity: {
 * @throws {FetchError} If the response is not OK.
 * @internal
 */
-declare function getRemoteDocument(url: string, response: Response, fetch: (url: string, options?: DocumentLoaderOptions) => Promise<RemoteDocument>): Promise<RemoteDocument>;
+declare function getRemoteDocument(url: string, response: Response, fetch: (url: string, options?: DocumentLoaderOptions) => Promise<RemoteDocument>, options?: DocumentLoaderOptions, maxResponseSize?: number): Promise<RemoteDocument>;
 /**
 * Options for {@link getDocumentLoader}.
 * @since 1.3.0
@@ -199,6 +204,7 @@ interface GetDocumentLoaderOptions extends DocumentLoaderFactoryOptions {
 declare function getDocumentLoader({
   allowPrivateAddress,
   maxRedirection,
+  maxResponseSize,
   skipPreloadedContexts,
   userAgent
 }?: GetDocumentLoaderOptions): DocumentLoader;
diff --git a/dist/mod.d.ts b/dist/mod.d.ts
index 4212039..de9b48f 100644
--- a/dist/mod.d.ts
+++ b/dist/mod.d.ts
@@ -144,6 +144,11 @@ interface DocumentLoaderFactoryOptions {
   * @since 2.2.0
   */
   maxRedirection?: number;
+  /**
+  * The maximum response body size in bytes.  If omitted, the response body is
+  * unbounded.
+  */
+  maxResponseSize?: number;
 }
 /**
 * A factory function that creates an authenticated {@link DocumentLoader} for
@@ -168,7 +173,7 @@ type AuthenticatedDocumentLoaderFactory = (identity: {
 * @throws {FetchError} If the response is not OK.
 * @internal
 */
-declare function getRemoteDocument(url: string, response: Response, fetch: (url: string, options?: DocumentLoaderOptions) => Promise<RemoteDocument>): Promise<RemoteDocument>;
+declare function getRemoteDocument(url: string, response: Response, fetch: (url: string, options?: DocumentLoaderOptions) => Promise<RemoteDocument>, options?: DocumentLoaderOptions, maxResponseSize?: number): Promise<RemoteDocument>;
 /**
 * Options for {@link getDocumentLoader}.
 * @since 1.3.0
@@ -199,6 +204,7 @@ interface GetDocumentLoaderOptions extends DocumentLoaderFactoryOptions {
 declare function getDocumentLoader({
   allowPrivateAddress,
   maxRedirection,
+  maxResponseSize,
   skipPreloadedContexts,
   userAgent
 }?: GetDocumentLoaderOptions): DocumentLoader;
diff --git a/dist/mod.js b/dist/mod.js
index 97ed08b..c42c144 100644
--- a/dist/mod.js
+++ b/dist/mod.js
@@ -4775,6 +4775,56 @@ const logger = getLogger([
 	"docloader"
 ]);
 const DEFAULT_MAX_REDIRECTION = 20;
+const MAX_HTML_SIZE = 1024 * 1024;
+class ResponseBodyTooLargeError extends Error {
+	constructor(maxResponseSize) {
+		super(`Response body exceeds the configured limit of ${maxResponseSize} bytes.`);
+		this.name = "ResponseBodyTooLargeError";
+	}
+}
+async function readResponseText(response, maxResponseSize) {
+	if (maxResponseSize == null) return await response.text();
+	const contentLength = response.headers.get("Content-Length");
+	const declaredSize = contentLength == null ? null : Number(contentLength);
+	if (declaredSize != null && Number.isSafeInteger(declaredSize) && declaredSize >= 0 && declaredSize > maxResponseSize) {
+		try {
+			await response.body?.cancel();
+		} catch {
+		}
+		throw new ResponseBodyTooLargeError(maxResponseSize);
+	}
+	const reader = response.body?.getReader();
+	if (reader == null) return await response.text();
+	const chunks = [];
+	let size = 0;
+	try {
+		while (true) {
+			const { done, value } = await reader.read();
+			if (done) break;
+			size += value.byteLength;
+			if (size > maxResponseSize) {
+				try {
+					await reader.cancel();
+				} catch {
+				}
+				throw new ResponseBodyTooLargeError(maxResponseSize);
+			}
+			chunks.push(value);
+		}
+	} finally {
+		reader.releaseLock();
+	}
+	const bytes = new Uint8Array(size);
+	let offset = 0;
+	for (const chunk of chunks) {
+		bytes.set(chunk, offset);
+		offset += chunk.byteLength;
+	}
+	return new TextDecoder().decode(bytes);
+}
+async function readResponseJson(response, maxResponseSize) {
+	return JSON.parse(await readResponseText(response, maxResponseSize));
+}
 /**
 * Gets a {@link RemoteDocument} from the given response.
 * @param url The URL of the document to load.
@@ -4784,7 +4834,7 @@ const DEFAULT_MAX_REDIRECTION = 20;
 * @throws {FetchError} If the response is not OK.
 * @internal
 */
-async function getRemoteDocument(url, response, fetch) {
+async function getRemoteDocument(url, response, fetch, options, maxResponseSize) {
 	const documentUrl = response.url === "" ? url : response.url;
 	const docUrl = new URL(documentUrl);
 	if (!response.ok) {
@@ -4822,15 +4872,14 @@ async function getRemoteDocument(url, response, fetch) {
 						alternateUrl: altUri.href,
 						url: documentUrl
 					});
-					return await fetch(altUri.href);
+					return await fetch(altUri.href, options);
 				}
 			}
 		}
 	}
 	let document;
 	if (!jsonLd && (contentType === "text/html" || contentType?.startsWith("text/html;") || contentType === "application/xhtml+xml" || contentType?.startsWith("application/xhtml+xml;"))) {
-		const MAX_HTML_SIZE = 1024 * 1024;
-		const html = await response.text();
+		const html = await readResponseText(response, Math.min(maxResponseSize ?? MAX_HTML_SIZE, MAX_HTML_SIZE));
 		if (html.length > MAX_HTML_SIZE) {
 			logger.warn("HTML response too large, skipping alternate link discovery: {url}", {
 				url: documentUrl,
@@ -4855,12 +4904,12 @@ async function getRemoteDocument(url, response, fetch) {
 						alternateUrl: attribs.href,
 						url: documentUrl
 					});
-					return await fetch(new URL(attribs.href, docUrl).href);
+					return await fetch(new URL(attribs.href, docUrl).href, options);
 				}
 			}
 			document = JSON.parse(html);
 		}
-	} else document = await response.json();
+	} else document = await readResponseJson(response, maxResponseSize);
 	logger.debug("Fetched document: {status} {url} {headers}", {
 		status: response.status,
 		url: documentUrl,
@@ -4889,7 +4938,7 @@ async function getRemoteDocument(url, response, fetch) {
 * @returns The document loader.
 * @since 1.3.0
 */
-function getDocumentLoader({ allowPrivateAddress, maxRedirection, skipPreloadedContexts, userAgent } = {}) {
+function getDocumentLoader({ allowPrivateAddress, maxRedirection, maxResponseSize, skipPreloadedContexts, userAgent } = {}) {
 	const tracer = trace.getTracerProvider().getTracer(name, version);
 	const maximumRedirection = maxRedirection ?? DEFAULT_MAX_REDIRECTION;
 	async function load(url, options, redirected = 0, visited = /* @__PURE__ */ new Set()) {
@@ -4944,7 +4993,7 @@ function getDocumentLoader({ allowPrivateAddress, maxRedirection, skipPreloadedC
 					}
 					return await load(redirectUrl, options, redirected + 1, visited);
 				}
-				const result = await getRemoteDocument(currentUrl, response, load);
+				const result = await getRemoteDocument(currentUrl, response, load, options, maxResponseSize);
 				span.setAttribute("docloader.document_url", result.documentUrl);
 				if (result.contextUrl != null) span.setAttribute("docloader.context_url", result.contextUrl);
 				return result;
diff --git a/src/docloader.ts b/src/docloader.ts
index 0cca8d6..a59f5d5 100644
--- a/src/docloader.ts
+++ b/src/docloader.ts
@@ -13,6 +13,7 @@ import { UrlError, validatePublicUrl } from "./url.ts";
 
 const logger = getLogger(["fedify", "runtime", "docloader"]);
 const DEFAULT_MAX_REDIRECTION = 20;
+const MAX_HTML_SIZE = 1024 * 1024;
 
 /**
  * A remote JSON-LD document and its context fetched by
@@ -95,6 +96,15 @@ export interface DocumentLoaderFactoryOptions {
    * @since 2.2.0
    */
   maxRedirection?: number;
+
+  /**
+   * The maximum number of response body bytes to read.
+   *
+   * The limit is enforced while streaming, after any content decoding done by
+   * the Fetch implementation.  If omitted, JSON-LD responses are unbounded;
+   * HTML alternate discovery retains its built-in 1 MiB limit.
+   */
+  maxResponseSize?: number;
 }
 
 /**
@@ -112,6 +122,74 @@ export type AuthenticatedDocumentLoaderFactory = (
   options?: DocumentLoaderFactoryOptions,
 ) => DocumentLoader;
 
+class ResponseBodyTooLargeError extends Error {
+  constructor(maxResponseSize: number) {
+    super(
+      `Response body exceeds the configured limit of ${maxResponseSize} bytes.`,
+    );
+    this.name = "ResponseBodyTooLargeError";
+  }
+}
+
+async function readResponseText(
+  response: Response,
+  maxResponseSize?: number,
+): Promise<string> {
+  if (maxResponseSize == null) return await response.text();
+
+  const contentLength = response.headers.get("Content-Length");
+  const declaredSize = contentLength == null ? undefined : Number(contentLength);
+  if (
+    declaredSize != null && Number.isSafeInteger(declaredSize) &&
+    declaredSize >= 0 && declaredSize > maxResponseSize
+  ) {
+    try {
+      await response.body?.cancel();
+    } catch {
+      // Preserve the size-limit error even if the runtime rejects cancellation.
+    }
+    throw new ResponseBodyTooLargeError(maxResponseSize);
+  }
+
+  const reader = response.body?.getReader();
+  if (reader == null) return "";
+  const chunks: Uint8Array[] = [];
+  let size = 0;
+  try {
+    while (true) {
+      const { done, value } = await reader.read();
+      if (done) break;
+      size += value.byteLength;
+      if (size > maxResponseSize) {
+        try {
+          await reader.cancel();
+        } catch {
+          // Preserve the size-limit error even if the runtime rejects cancellation.
+        }
+        throw new ResponseBodyTooLargeError(maxResponseSize);
+      }
+      chunks.push(value);
+    }
+  } finally {
+    reader.releaseLock();
+  }
+
+  const bytes = new Uint8Array(size);
+  let offset = 0;
+  for (const chunk of chunks) {
+    bytes.set(chunk, offset);
+    offset += chunk.byteLength;
+  }
+  return new TextDecoder().decode(bytes);
+}
+
+async function readResponseJson(
+  response: Response,
+  maxResponseSize?: number,
+): Promise<unknown> {
+  return JSON.parse(await readResponseText(response, maxResponseSize));
+}
+
 /**
  * Gets a {@link RemoteDocument} from the given response.
  * @param url The URL of the document to load.
@@ -128,6 +206,8 @@ export async function getRemoteDocument(
     url: string,
     options?: DocumentLoaderOptions,
   ) => Promise<RemoteDocument>,
+  options?: DocumentLoaderOptions,
+  maxResponseSize?: number,
 ): Promise<RemoteDocument> {
   const documentUrl = response.url === "" ? url : response.url;
   const docUrl = new URL(documentUrl);
@@ -188,7 +268,7 @@ export async function getRemoteDocument(
             "Found alternate document: {alternateUrl} from {url}",
             { alternateUrl: altUri.href, url: documentUrl },
           );
-          return await fetch(altUri.href);
+          return await fetch(altUri.href, options);
         }
       }
     }
@@ -200,57 +280,50 @@ export async function getRemoteDocument(
       contentType === "application/xhtml+xml" ||
       contentType?.startsWith("application/xhtml+xml;"))
   ) {
-    // Security: Limit HTML response size to mitigate ReDoS attacks
-    const MAX_HTML_SIZE = 1024 * 1024; // 1MB
-    const html = await response.text();
-    if (html.length > MAX_HTML_SIZE) {
-      logger.warn(
-        "HTML response too large, skipping alternate link discovery: {url}",
-        { url: documentUrl, size: html.length },
-      );
-      document = JSON.parse(html);
-    } else {
-      // Safe regex patterns without nested quantifiers to prevent ReDoS
-      // (CVE-2025-68475)
-      // Step 1: Extract <a ...> or <link ...> tags
-      const tagPattern = /<(a|link)\s+([^>]*?)\s*\/?>/gi;
-      // Step 2: Parse attributes
-      const attrPattern =
-        /([a-z][a-z:_-]*)=(?:"([^"]*)"|'([^']*)'|([^\s>]+))/gi;
+    const html = await readResponseText(
+      response,
+      Math.min(maxResponseSize ?? MAX_HTML_SIZE, MAX_HTML_SIZE),
+    );
+    // Safe regex patterns without nested quantifiers to prevent ReDoS
+    // (CVE-2025-68475)
+    // Step 1: Extract <a ...> or <link ...> tags
+    const tagPattern = /<(a|link)\s+([^>]*?)\s*\/?>/gi;
+    // Step 2: Parse attributes
+    const attrPattern =
+      /([a-z][a-z:_-]*)=(?:"([^"]*)"|'([^']*)'|([^\s>]+))/gi;
 
-      let tagMatch: RegExpExecArray | null;
-      while ((tagMatch = tagPattern.exec(html)) !== null) {
-        const tagContent = tagMatch[2];
-        let attrMatch: RegExpExecArray | null;
-        const attribs: Record<string, string> = {};
+    let tagMatch: RegExpExecArray | null;
+    while ((tagMatch = tagPattern.exec(html)) !== null) {
+      const tagContent = tagMatch[2];
+      let attrMatch: RegExpExecArray | null;
+      const attribs: Record<string, string> = {};
 
-        // Reset regex state for attribute parsing
-        attrPattern.lastIndex = 0;
-        while ((attrMatch = attrPattern.exec(tagContent)) !== null) {
-          const key = attrMatch[1].toLowerCase();
-          const value = attrMatch[2] ?? attrMatch[3] ?? attrMatch[4] ?? "";
-          attribs[key] = value;
-        }
+      // Reset regex state for attribute parsing
+      attrPattern.lastIndex = 0;
+      while ((attrMatch = attrPattern.exec(tagContent)) !== null) {
+        const key = attrMatch[1].toLowerCase();
+        const value = attrMatch[2] ?? attrMatch[3] ?? attrMatch[4] ?? "";
+        attribs[key] = value;
+      }
 
-        if (
-          attribs.rel === "alternate" && "type" in attribs && (
-            attribs.type === "application/activity+json" ||
-            attribs.type === "application/ld+json" ||
-            attribs.type.startsWith("application/ld+json;")
-          ) && "href" in attribs &&
-          new URL(attribs.href, docUrl).href !== docUrl.href
-        ) {
-          logger.debug(
-            "Found alternate document: {alternateUrl} from {url}",
-            { alternateUrl: attribs.href, url: documentUrl },
-          );
-          return await fetch(new URL(attribs.href, docUrl).href);
-        }
+      if (
+        attribs.rel === "alternate" && "type" in attribs && (
+          attribs.type === "application/activity+json" ||
+          attribs.type === "application/ld+json" ||
+          attribs.type.startsWith("application/ld+json;")
+        ) && "href" in attribs &&
+        new URL(attribs.href, docUrl).href !== docUrl.href
+      ) {
+        logger.debug(
+          "Found alternate document: {alternateUrl} from {url}",
+          { alternateUrl: attribs.href, url: documentUrl },
+        );
+        return await fetch(new URL(attribs.href, docUrl).href, options);
       }
-      document = JSON.parse(html);
     }
+    document = JSON.parse(html);
   } else {
-    document = await response.json();
+    document = await readResponseJson(response, maxResponseSize);
   }
   logger.debug(
     "Fetched document: {status} {url} {headers}",
@@ -292,7 +365,13 @@ export interface GetDocumentLoaderOptions extends DocumentLoaderFactoryOptions {
  * @since 1.3.0
  */
 export function getDocumentLoader(
-  { allowPrivateAddress, maxRedirection, skipPreloadedContexts, userAgent }:
+  {
+    allowPrivateAddress,
+    maxRedirection,
+    maxResponseSize,
+    skipPreloadedContexts,
+    userAgent,
+  }:
     GetDocumentLoaderOptions = {},
 ): DocumentLoader {
   const tracerProvider = trace.getTracerProvider();
@@ -385,7 +464,13 @@ export function getDocumentLoader(
             return await load(redirectUrl, options, redirected + 1, visited);
           }
 
-          const result = await getRemoteDocument(currentUrl, response, load);
+          const result = await getRemoteDocument(
+            currentUrl,
+            response,
+            load,
+            options,
+            maxResponseSize,
+          );
           span.setAttribute("docloader.document_url", result.documentUrl);
           if (result.contextUrl != null) {
             span.setAttribute("docloader.context_url", result.contextUrl);
